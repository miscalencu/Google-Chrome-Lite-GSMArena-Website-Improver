function setDefaultVariables()
	{
	if(!localStorage.highlightCompareDifferences)
		localStorage.highlightCompareDifferences = 1;
		
	if(!localStorage.useGallery)
		localStorage.useGallery = 1;
	}

setDefaultVariables();